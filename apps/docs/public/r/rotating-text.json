{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "rotating-text",
  "type": "registry:ui",
  "title": "Rotating Text",
  "description": "Cycles through phrases in place with slide or fade motion. Polite aria-live announces the current word, pauses on hover/focus, and keeps a stable layout. Reduced-motion safe.",
  "dependencies": [
    "motion"
  ],
  "registryDependencies": [
    "https://motiq.dev/r/utils.json"
  ],
  "files": [
    {
      "path": "registry/text/rotating-text.tsx",
      "type": "registry:ui",
      "target": "components/motiq/rotating-text.tsx",
      "content": "\"use client\";\n\nimport * as React from \"react\";\nimport { AnimatePresence, motion, useReducedMotion } from \"motion/react\";\n\nimport { cn } from \"@/lib/utils\";\n\ntype TransitionKind = \"slide\" | \"fade\";\n\nexport interface RotatingTextProps\n  extends Omit<React.HTMLAttributes<HTMLSpanElement>, \"children\"> {\n  /** Phrases to cycle through. */\n  words: string[];\n  /** Milliseconds each word is shown. Guarded to a 1200ms minimum. */\n  interval?: number;\n  /** Motion style between words. */\n  transition?: TransitionKind;\n}\n\n/**\n * RotatingText — cycles through an array of words in place.\n *\n * Accessibility: an `aria-live=\"polite\"` region announces only the current\n * word, so screen readers hear the change without the animated duplicates.\n * Cycling pauses on hover; the element is intentionally not focusable (no\n * role-less tab stop). Layout is stable (inline-grid stacks on one cell). Under\n * `prefers-reduced-motion` the text swaps with no transform. Clean-room\n * original.\n */\nexport function RotatingText({\n  words,\n  interval = 2200,\n  transition = \"slide\",\n  className,\n  ...props\n}: RotatingTextProps) {\n  const reduce = useReducedMotion();\n  const [index, setIndex] = React.useState(0);\n  const [paused, setPaused] = React.useState(false);\n\n  const safeWords = words.length > 0 ? words : [\"\"];\n  const delay = Math.max(1200, interval);\n\n  React.useEffect(() => {\n    if (paused || safeWords.length <= 1) return;\n    const id = window.setTimeout(() => {\n      setIndex((i) => (i + 1) % safeWords.length);\n    }, delay);\n    return () => window.clearTimeout(id);\n  }, [index, paused, delay, safeWords.length]);\n\n  // Keep the index valid if the words array shrinks.\n  React.useEffect(() => {\n    if (index >= safeWords.length) setIndex(0);\n  }, [index, safeWords.length]);\n\n  const current = safeWords[index] ?? \"\";\n  const y = transition === \"slide\" ? \"0.9em\" : 0;\n\n  return (\n    <span\n      className={cn(\"relative inline-grid align-bottom\", className)}\n      // Pause on hover so readers can keep up. We deliberately do NOT make this a\n      // focusable element — a role-less tabbable span is an a11y smell (WCAG\n      // 4.1.2); keyboard users are served by the polite live region + reduced-motion.\n      onMouseEnter={() => setPaused(true)}\n      onMouseLeave={() => setPaused(false)}\n      {...props}\n    >\n      {/* Polite live region: announces the current word only. */}\n      <span aria-live=\"polite\" className=\"sr-only\">\n        {current}\n      </span>\n\n      <AnimatePresence initial={false} mode=\"popLayout\">\n        <motion.span\n          key={index}\n          aria-hidden=\"true\"\n          // All words share cell [1/1/1/1] so width follows the widest via grid.\n          className=\"col-start-1 row-start-1 inline-block whitespace-nowrap [will-change:transform,opacity]\"\n          initial={reduce ? false : { opacity: 0, y }}\n          animate={{ opacity: 1, y: 0 }}\n          exit={reduce ? { opacity: 0 } : { opacity: 0, y: `-${y}` }}\n          transition={{ duration: reduce ? 0 : 0.32, ease: [0.2, 0, 0, 1] }}\n        >\n          {current}\n        </motion.span>\n      </AnimatePresence>\n    </span>\n  );\n}\n\nexport default RotatingText;\n"
    }
  ],
  "meta": {
    "tier": "free",
    "category": "text",
    "keywords": [
      "text",
      "rotating",
      "words",
      "hero",
      "headline"
    ]
  },
  "cssVars": {
    "light": {
      "color-accent": "#315fea",
      "color-accent-fg": "#ffffff",
      "color-accent-hover": "#244fd1",
      "color-accent-text": "#244fd1",
      "color-bg": "#f7f9fc",
      "color-bg-elevated": "#f0f4f9",
      "color-bg-secondary": "#f0f4f9",
      "color-border": "#dce4ef",
      "color-border-strong": "#c5d1e1",
      "color-code-bg": "#f0f4f9",
      "color-code-fg": "#2a3444",
      "color-error": "#d92d4a",
      "color-error-foreground": "#ffffff",
      "color-fg": "#101828",
      "color-fg-secondary": "#344054",
      "color-focus": "#315fea",
      "color-info": "#087dbd",
      "color-info-foreground": "#ffffff",
      "color-muted": "#667085",
      "color-neutral": "#475467",
      "color-neutral-foreground": "#ffffff",
      "color-secondary-accent": "#009fb3",
      "color-signature": "#e9564a",
      "color-success": "#128a55",
      "color-success-foreground": "#ffffff",
      "color-surface": "#ffffff",
      "color-surface-2": "#f8fafd",
      "color-warning": "#b86e00",
      "color-warning-foreground": "#ffffff"
    },
    "dark": {
      "color-accent": "#4f7cff",
      "color-accent-fg": "#080c14",
      "color-accent-hover": "#6f91ff",
      "color-accent-text": "#7f9fff",
      "color-bg": "#080c14",
      "color-bg-elevated": "#0d1420",
      "color-bg-secondary": "#0d1420",
      "color-border": "#263449",
      "color-border-strong": "#354863",
      "color-code-bg": "#0d1420",
      "color-code-fg": "#cbd5e1",
      "color-error": "#ff5c70",
      "color-error-foreground": "#2a0308",
      "color-fg": "#f8fafc",
      "color-fg-secondary": "#cbd5e1",
      "color-focus": "#4f7cff",
      "color-info": "#38bdf8",
      "color-info-foreground": "#04121f",
      "color-muted": "#9caabd",
      "color-neutral": "#b2bccd",
      "color-neutral-foreground": "#111827",
      "color-secondary-accent": "#22c7d9",
      "color-signature": "#ff6b5e",
      "color-success": "#32d583",
      "color-success-foreground": "#04150c",
      "color-surface": "#111827",
      "color-surface-2": "#192337",
      "color-warning": "#f6b94a",
      "color-warning-foreground": "#2a1a00"
    }
  }
}
